import { useState } from "react"

export interface centerDetails {
    isLoading: boolean,
    //movieFull:??
    cast:any[],


}
export const useCenterDetails = () => {

        //const [state, setstate] = useState<centerDetails>(initialState)
 
}
